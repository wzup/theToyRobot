'use strict';

module.exports = Messenger;

function Messenger (config) {
    this._aMsgs = config._aMsgs;
    this._Directions
};

Messenger.prototype.getMessege = function(argv) {
    return msg;
};

Messenger.prototype._constructMessage = function() {

};
