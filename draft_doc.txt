1. как запускать npm lint, чтобы в режиме debug?
я там в файле shibang прописываю, мож в нем прописать что-то надо

2. Скачать jasmine и тестить

3. как из Robot.prototype.move метода
получить значение переменной
var aCommands = ['place', 'move', 'left', 'right', 'report'];
Неужели из прототайпных методов нет доступа к таким переменным?
Как получить? как обойти? Может спросить на
подумать может как-то через интерфейс, типа RobotInterface - методы без реализаций
Robot - уже класс, наследующий интерфейс

Один и тот же ли это метод будет?
RobotInterface.prototype.move
Robot.prototype.move
Robot() {
    this.move
}

4. Подумать как с сообщениями
report - выводит все или для ошибок свой метод .warning() ?

5. DI
Робот использует Playground
Подумать как реализовать

6. Как дебажить debugger в файлах с экспортом



